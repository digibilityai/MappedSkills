# Wireframe — VARIANT 01 — High-Scepticism Commercial (V1, `/ai-seo`) — of W2 — PROPOSED

**Session:** 13 — Gate 9B completion pass · **Date:** 2026-09-02 · **Branch:** `test_branch`
**Status:** **PROPOSED.** Legend: `30_WIREFRAME_LEGEND.md`. Inherits `32_WIREFRAME_MASTER_COMMERCIAL.md` (W2).
**Source of structure:** `COMMERCIAL_PAGE_MESSAGE_PATTERN.md` §1.1–§1.2 (`/ai-seo` = **all nine moves**) · `05_COMMERCIAL_PAGE_UX.md` §4.2 · `23_COMMERCIAL_PAGE_DIFFERENTIATION_TEST.md` §3 (as corrected 2026-09-02) · `22_F1_USAGE_TAXONOMY.md` §3 (`/ai-seo` = D — NONE, reclassified).

> **CONSEQUENTIAL CORRECTION 2026-09-02 (remediation pass).** W2 (`32`) was re-authored to the approved move taxonomy; this variant is re-checked against it. **Two moves were missing from the diagram** — move 4 (what the work involves) and move 6 (how it is measured) — even though `/ai-seo` carries **all nine**. They are restored below. **The evidence-before-boundary order was already correct here and is unchanged**; orchestrator ruling A confirms `05` §4.2 controls that sequence, and the contradicting row in `23` §3 was corrected rather than this file. **No move was normalised toward W2's arrangement: this page's differences are the point of it.**

---

## Margin notes

- **Page type:** Commercial page, variant of W2. **Structural difference:** evidence as an early named section **placed before the boundary**, F3 full weight, NO run, objections as a section. **The longest commercial page on the site.**
- **Intent-driven variation (`05` §3 drivers, stated not coded):** **sophistication** — mixed, so the work is described rather than assumed · **scepticism** — very high, but about whether any of this is real · **misinformation** — the highest on the site: the market conflates training crawlers with retrieval crawlers, so **objections are a section of factual corrections, and a corrected buyer is a converted one** · **dispute risk** — what can be influenced and what can be measured at all, which is why move 5 is the strongest boundary on the site.
- **First screen:** identical spine to W2 — confirm + bridge + [ACT], zero apparatus (I2 holds without exception even here).
- **F1 class:** **D — NONE.** F3 (four AI layers) is this page's figure and carries full weight; adding the run would be a second full-weight figure, prohibited (`22` §3).
- **CTA roles/locations:** same 2 [ACT] + 1 [SEC/PEO] DIRECT budget as W2 (I7). No extra CTA density despite added length — R2 still governs (position follows the argument, not page length).
- **Omitted slots:** none owner-blocked. One `[VALIDATION REQUIRED]` claim flagged per `03` §3 dependencies (a content/evidence-status flag, not a blocked slot).
- **Patterns used (`49_UX_PATTERN_INVENTORY.md` IDs):** P1 · P4 · P5 · P8 · P12 · P13 *(F3)* · R1 · R2. **P3 is NOT used** — `/ai-seo` is D — NONE, so the run wrapper does not render here at all.
- **Width class shown:** WIDE (primary). NARROW: see `48` §M7 (six-system comparison at narrow width — scroll container, page body never scrolls horizontally).
- **What this artefact uniquely proves:** evidence appearing earlier is a comprehension mechanism, not a reassurance move — and it still never appears above the fold (I2 unbroken even on the highest-scepticism page); this is one of the two D-class proofs the taxonomy requires (`22` §7).

---

## WIDE structural diagram — differences from W2 only

```
┌───────────────────────────────────────────────────────────┐
│ MOVE 1+2 — OPENER  (as W2: confirm + bridge + [ACT])         │
│  [EVD: ABSENT — I2 holds without exception]                   │
├───────────────────────────────────────────────────────────┤
│ MOVE 3 — THE BUYER'S ACTUAL PROBLEM                [ARG]    │
│  the misinformation named — someone has already sold this      │
│  buyer a score                                                  │
├───────────────────────────────────────────────────────────┤
│ MOVE 4 — WHAT THE WORK ACTUALLY INVOLVES           [ARG]    │
│  present at full weight — /ai-seo carries all nine moves        │
│  (`COMMERCIAL_PAGE_MESSAGE_PATTERN.md` §1.2), unlike W2 where   │
│  brevity here is the sophistication driver                       │
├───────────────────────────────────────────────────────────┤
│ MOVE 7 — EVIDENCE, AS A NAMED SECTION (NOT compressed)         │
│                                          [FIG: F3, full weight]│
│  ** BEFORE the boundary — ruling A, `05`§4.2 controls **        │
│  four AI layers kept separate, per system · two systems marked │
│  NOT TESTABLE — visually distinct from a measured zero,         │
│  distinguishable without colour (B1) · layers NEVER aggregated  │
│  into a score (B7)                                               │
│  [EVD: this section is the ARGUMENT itself, not reassurance.    │
│   The figure is early because the argument needs it; the        │
│   credibility effect is a by-product (`05`§4.2). Still NOT      │
│   above the fold — I2 unbroken]                                  │
├───────────────────────────────────────────────────────────┤
│ MOVE 5 — BOUNDARY, EARLY, FULL STRENGTH             [LIM]    │
│  the strongest boundary on the site: what can/cannot be         │
│  influenced · what cannot be measured · which systems could     │
│  not be tested at all                                            │
│  [EVD: ADJACENT-SECONDARY · ALWAYS OPEN · NEVER COLLAPSIBLE]     │
│  I4 satisfied: it limits the layers the reader has just seen     │
├───────────────────────────────────────────────────────────┤
│ MOVE 6 — HOW IT IS MEASURED, AND WHAT WILL BE SHOWN  [ARG]   │
│  including what cannot be attributed                             │
│  ── measurement limits, ADJACENT and OPEN ──          [LIM]      │
│  one round, one location, English, signed out, two of six        │
│  named systems untestable — beside the measurement claim,        │
│  never collapsible, never below the CTA (`10`§5)                  │
│  NO RUN appears here (D-class) — unlike W2, whose F1 PARTIAL      │
│  sits at this move                                                 │
├───────────────────────────────────────────────────────────┤
│ MOVE 8 — OBJECTIONS, A NAMED SECTION (not a sentence)  [ARG]  │
│  factual corrections to category misinformation                  │
│  [SEC] PROOF → /how-it-works · [SEC] CONTEXTUAL · [SEC] up →     │
│  /services  (I6, I9)                                              │
├───────────────────────────────────────────────────────────┤
│ MOVE 9 — ACT  (as W2: 2 [ACT] + 1 [SEC/PEO] DIRECT)            │
│  [EVD: ABSENT — I8]                                              │
└───────────────────────────────────────────────────────────┘
```

**Move order, for the clone check in `50` §2:**
`confirm → bridge → problem → work → **evidence section (F3)** → **strong boundary** → measurement → **objections section** → convert`
— matching `23` §3's corrected `/ai-seo` row.

**Moves present and absent, against `COMMERCIAL_PAGE_MESSAGE_PATTERN.md` §1.2:**

| Move | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|---|---|---|
| `/ai-seo` | ✓ | ✓ | ✓ | ✓ | ✓ **strongest** | ✓ | ✓ **named section, early** | ✓ **section** | ✓ |

**All nine present. Nothing is compressed on this page** — that is what makes it the longest of the six, and the length is the consequence of the intent, not of padding.

**All other blocks (opener, scope, up/across links, footer) inherit W2 unchanged.**

**Prohibited on this wireframe (checked):** no [FIG] run anywhere on this page (D-class, `22` §5), no aggregated AI-layer score (B7), no colour-only not-testable/zero distinction (B1, F5 test), no evidence section placed above the fold despite its early position (I2), no collapsible boundary or measurement limit at any width (I5, B29), no rejected acronym (GEO/AEO/LLMO) in any heading, caption or label (`DEC-013`), no reintroduction of the removed AI-referral-share statistic, no blocked slot drawn on the artboard (`30`§4).
